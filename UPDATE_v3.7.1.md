# Meal Violations Dashboard 3.7.1 — Header fix

Corrección visual puntual:

- Logo reducido y centrado dentro de un contenedor propio.
- Encabezado con altura mínima y padding suficientes.
- `overflow: visible` para evitar recortes.
- Card al 100% del ancho disponible con `box-sizing: border-box`.
- Ajustes responsive para escritorio y móvil.
- Versión mostrada por la aplicación: 3.7.1.

## Instalación

Reemplazar `app.py` en la raíz del repositorio y hacer push:

```powershell
git add app.py
git commit -m "Fix dashboard header and logo sizing"
git push origin main
```
